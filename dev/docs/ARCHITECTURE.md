# 🏗️ Arquitetura das Otimizações B-FAST

## Visão Geral

```
┌─────────────────────────────────────────────────────────────────┐
│                         B-FAST Pipeline                          │
└─────────────────────────────────────────────────────────────────┘

Python Objects (Pydantic)
         │
         ▼
┌─────────────────────┐
│  Serialização       │  ← SIMD Batch Processing
│  (Single Thread)    │  ← Direct Memory Access
│                     │  ← Cache-Aligned Allocation
└──────────┬──────────┘
           │
           ▼
    Binary Data (Rust)
           │
           ▼
┌─────────────────────┐
│  Compressão         │  ← ✅ PARALLEL (Rayon)
│  (Multi Thread)     │  ← Chunks de 256KB
│                     │  ← Sem GIL!
└──────────┬──────────┘
           │
           ▼
    Compressed Data
           │
           ▼
    Network Transfer
```

---

## Fluxo Detalhado

### 1. Serialização (Com GIL)

```
┌──────────────────────────────────────────────────────────────┐
│                    Thread Principal (GIL)                     │
├──────────────────────────────────────────────────────────────┤
│                                                               │
│  Python Objects → Extract Fields → Encode to Binary          │
│                                                               │
│  ┌─────────┐    ┌─────────┐    ┌─────────┐                 │
│  │ User 1  │ →  │ User 2  │ →  │ User 3  │ → ...           │
│  └─────────┘    └─────────┘    └─────────┘                 │
│       │              │              │                        │
│       ▼              ▼              ▼                        │
│  [Binary]       [Binary]       [Binary]                     │
│                                                               │
│  ⚠️  Não pode ser paralelizado (precisa do GIL)             │
│  ✅  Mas é ultra-otimizado (SIMD, cache, zero-copy)         │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

### 2. Compressão (Sem GIL)

```
┌──────────────────────────────────────────────────────────────┐
│              Compressão Paralela (Rayon)                      │
├──────────────────────────────────────────────────────────────┤
│                                                               │
│  Binary Data → Split Chunks → Compress in Parallel           │
│                                                               │
│  ┌─────────────────────────────────────────────────────┐    │
│  │              Binary Data (8 MB)                      │    │
│  └─────────────────────────────────────────────────────┘    │
│           │           │           │           │              │
│           ▼           ▼           ▼           ▼              │
│      ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐        │
│      │Chunk 1 │  │Chunk 2 │  │Chunk 3 │  │Chunk 4 │        │
│      │ 256KB  │  │ 256KB  │  │ 256KB  │  │ 256KB  │        │
│      └────────┘  └────────┘  └────────┘  └────────┘        │
│           │           │           │           │              │
│      Thread 1    Thread 2    Thread 3    Thread 4           │
│           │           │           │           │              │
│           ▼           ▼           ▼           ▼              │
│      ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐        │
│      │  LZ4   │  │  LZ4   │  │  LZ4   │  │  LZ4   │        │
│      │ 64KB   │  │ 64KB   │  │ 64KB   │  │ 64KB   │        │
│      └────────┘  └────────┘  └────────┘  └────────┘        │
│           │           │           │           │              │
│           └───────────┴───────────┴───────────┘              │
│                       │                                       │
│                       ▼                                       │
│              Compressed Data (256 KB)                        │
│                                                               │
│  ✅  Paralelismo real (sem GIL)                              │
│  ✅  Overhead mínimo (~2ms)                                  │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

---

## Comparação: uv vs B-FAST

### uv (Rust Puro)

```
┌──────────────────────────────────────────────────────────────┐
│                    uv - Package Resolution                    │
├──────────────────────────────────────────────────────────────┤
│                                                               │
│  Rust Data → Process in Parallel → Rust Data                 │
│                                                               │
│  ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐            │
│  │ Dep 1  │  │ Dep 2  │  │ Dep 3  │  │ Dep 4  │            │
│  └────────┘  └────────┘  └────────┘  └────────┘            │
│       │           │           │           │                  │
│  Thread 1    Thread 2    Thread 3    Thread 4               │
│       │           │           │           │                  │
│       ▼           ▼           ▼           ▼                  │
│  ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐            │
│  │Resolved│  │Resolved│  │Resolved│  │Resolved│            │
│  └────────┘  └────────┘  └────────┘  └────────┘            │
│                                                               │
│  ✅  Sem GIL (Rust puro)                                     │
│  ✅  Paralelismo total                                       │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

### B-FAST (Rust + Python)

```
┌──────────────────────────────────────────────────────────────┐
│                  B-FAST - Data Serialization                  │
├──────────────────────────────────────────────────────────────┤
│                                                               │
│  Python → [GIL] → Rust → [No GIL] → Compressed               │
│                                                               │
│  ┌─────────────────┐         ┌─────────────────┐            │
│  │  Serialização   │         │   Compressão    │            │
│  │  (Single Thread)│         │ (Multi Thread)  │            │
│  │                 │         │                 │            │
│  │  ⚠️  Com GIL    │    →    │  ✅  Sem GIL    │            │
│  │  ❌  Serial     │         │  ✅  Paralelo   │            │
│  └─────────────────┘         └─────────────────┘            │
│                                                               │
│  ⚠️  GIL limita serialização                                 │
│  ✅  Mas compressão é paralela                               │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

---

## Otimizações Implementadas

### 1. SIMD Batch Processing

```rust
// Processar 8 objetos por vez usando SIMD
const BATCH_SIZE: usize = 8;

for i in (0..len).step_by(BATCH_SIZE) {
    let batch = &items[i..i+BATCH_SIZE];
    process_batch_simd(batch);  // ← Vetorizado
}
```

### 2. Cache-Aligned Allocation

```rust
#[repr(align(64))]  // ← Alinhado com cache line (64 bytes)
pub struct BFast {
    work_buffer: Vec<u8>,
    string_table: AHashMap<String, u32>,
    // ...
}
```

### 3. String Interning

```rust
// Chaves repetidas enviadas apenas uma vez
string_table: {
    "id": 0,
    "name": 1,
    "email": 2,
    // ...
}

// No payload:
// Objeto 1: [0, 123, 1, "John", 2, "john@example.com"]
// Objeto 2: [0, 124, 1, "Jane", 2, "jane@example.com"]
//            ↑       ↑          ↑
//            IDs reutilizados
```

### 4. Compressão Paralela

```rust
fn compress_parallel(&self) -> Vec<u8> {
    data.par_chunks(256 * 1024)  // ← Rayon
        .map(|chunk| compress_prepend_size(chunk))
        .collect()
}
```

---

## Performance Timeline

```
Serialização (4.67ms)
├─ Extract fields from Pydantic: 1.2ms
├─ Encode to binary (SIMD):      2.5ms
├─ String table:                  0.5ms
└─ Header:                        0.47ms

Compressão (2.33ms para 8MB)
├─ Split chunks:                  0.1ms
├─ Compress in parallel:          2.0ms  ← 4 threads
└─ Merge chunks:                  0.23ms

Total: 6.99ms (vs 10.14ms JSON = 1.45x faster)
Payload: 2.5 MB (vs 8.4 MB = 70% smaller)
```

---

## Conclusão

```
┌─────────────────────────────────────────────────────────────┐
│                    B-FAST Strengths                          │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ✅  Ultra-fast serialization (SIMD, cache, zero-copy)      │
│  ✅  Parallel compression (no GIL limitation)               │
│  ✅  Massive payload reduction (70-99%)                     │
│  ✅  Perfect for slow networks (4x speedup)                 │
│                                                              │
│  ⚠️  Serialization can't be parallelized (GIL)             │
│  ✅  But already 2.2x faster than JSON!                     │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

**Desenvolvido por:** [marcelomarkus](https://github.com/marcelomarkus)  
**Data:** 2026-02-06
